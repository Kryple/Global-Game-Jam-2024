namespace Core.Observer_Pattern
{
    public enum IEvent
    {
        OnEnemyGetHurt,
        OnEnemyDie,
        OnPlayerGetHurt,
        OnPlayerDie,
        OnPlayerinRange,
        OnGamePause,
        OnGameStart
    }
}